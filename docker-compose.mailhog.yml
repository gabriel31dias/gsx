services:
  mailhog:
    image: mailhog/mailhog:v1.0.1
    container_name: gsx-mailhog
    restart: unless-stopped
    environment:
      MH_STORAGE: maildir
      MH_MAILDIR_PATH: /mailhog
    ports:
      - "1025:1025"
      - "8025:8025"
    volumes:
      - ./mailhog:/mailhog
